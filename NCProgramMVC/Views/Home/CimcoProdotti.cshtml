@model NCProgramMVC.Models.Cimco

@{
    ViewBag.Title = "Details";
}
<div class="container body-content">

    <h2 class="text-center"><img class="img-prodotti-xs pull-left" src="~/Content/Immagini/cimco.png" /><a href="@Url.Action("Cimco", "Home")">@Html.DisplayFor(model => model.Prodotto) </a></h2>
    <hr class="hrl" />
    <ul class="ncbreadcrumb">
        <li><a href="@Url.Action("Cimco", "Home")">CIMCO</a></li>
        <li><a href="@Url.Action("CimcoProdotti", "Home", new {id = Model.Cimco_Id })">@Html.DisplayFor(model => model.Prodotto)</a></li>
    </ul>
    
    
    <hr class="hr2" />
    <table class="table table-hover">

        @foreach (var item in Model.Cimcos.OrderBy(s => s.Posizione).Select((value, i) => new { i, value }))
        {
            <tr>
                <td>
                    @Html.ActionLink(@item.value.ProdottoDett, "CimcoDett", "Home", new { id = item.value.CimcoDett_Id }, null)
                </td>
            </tr>
        }
    </table>
    <div>
        @Html.Raw(Model.Descrizione)
    </div>
</div>
