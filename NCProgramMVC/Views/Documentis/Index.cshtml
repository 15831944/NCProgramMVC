@model IEnumerable<NCProgramMVC.Models.Documenti>

@{
    ViewBag.Title = "Index";
}
<div class="container body-content">

    <h2>Index</h2>
    <hr />
    <div class="container body-content">

        <p>
            @Html.ActionLink("Create New", "Create")
        </p>
        <table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Titolo)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Descrizione)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Nomefile)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Riservato)
                </th>
                <th></th>
            </tr>

            @foreach (var item in Model) {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Titolo)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Descrizione)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Nomefile)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Riservato)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id=item.Documenti_Id }) |
                        @Html.ActionLink("Details", "Details", new { id=item.Documenti_Id }) |
                        @Html.ActionLink("Delete", "Delete", new { id=item.Documenti_Id })
                    </td>
                </tr>
}

        </table>
    </div>
</div>
