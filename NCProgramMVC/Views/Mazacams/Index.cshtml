@model IEnumerable<NCProgramMVC.Models.Mazacam>

@{
    ViewBag.Title = "Index";
}
<div class="container body-content">

    <h2 class="text-center">Prodotti MAZACAM</h2>
    <hr class="hrl" />

    <p>
        <a href="@Url.Action("Create", "Mazacams")" class="btn btn-primary"><span class="badge">@ViewBag.ProdottiCount</span> Nuovo</a>

    </p>
    <table class="table table-hover">
        <thead>
            <tr>
                <th></th>
                <th></th>
                <th>Img</th>
                <th>
                    @Html.DisplayNameFor(model => model.Prodotto)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Descrizione)
                </th>
            </tr>

        </thead>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <button class="btn btn-success" type="button" data-toggle="collapse" data-target="#@item.Mazacam_Id" aria-expanded="false" aria-controls="@item.Mazacam_Id">
                        <span class="fa fa-chevron-down"> <strong>@item.Mazacams.Count()</strong></span>
                    </button>
                </td>
                <td>
                    <a class="btn btn-primary" href="@Url.Action("Edit", "Mazacams", new { id = @item.Mazacam_Id})"><span class="fa fa-pencil"></span></a>
                    @if (@item.Mazacams.Count() == 0)
                    {
                        <a class="btn btn-danger btn-xs" href="@Url.Action("Delete", "Mazacams", new { id = @item.Mazacam_Id})"><span class="fa fa-remove"></span></a>
                    }

                </td>
                <td>
                    <a href="@Url.Action("EditImg", "Mazacams", new {id = @item.Mazacam_Id })">
                        <img src="@Url.Content("~/Content/Immagini/Mazacam/" + @item.Mazacam_Id + ".png" )" class="img-servizio-sm" />
                    </a>

                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Prodotto)
                    <!---Area collapse per dettaglio prodotti MAZACAM-->
                    <div class="collapse" id="@item.Mazacam_Id">
                        <a class="btn btn-warning btn-sm" href="@Url.Action("Create", "MazacamDetts", new { prodotto = @item.Mazacam_Id})">Nuovo <span class="fa fa-plus"></span></a>
                        <table class="table table-hover well">
                            @foreach (var item1 in item.Mazacams)
                            {
                                <tr>
                                    <td>
                                        <a class="btn btn-primary" href="@Url.Action("Edit", "MazacamDetts", new { id = @item1.MazacamDett_Id})"><span class="fa fa-pencil "></span></a>
                                        <a class="btn btn-danger btn-xs" href="@Url.Action("Delete", "MazacamDetts", new { id = @item1.MazacamDett_Id})"><span class="fa fa-remove "></span></a>
                                        @item1.ProdottoDett


                                    </td>
                                </tr>
                            }
                        </table>
                    </div>
                </td>
                <td>
                    @Html.Raw(item.Descrizione)
                </td>
            </tr>
        }

    </table>
</div>